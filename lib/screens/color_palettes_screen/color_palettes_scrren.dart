import 'package:flutter/material.dart';

class ColorPalettesScrren extends StatelessWidget {
  const ColorPalettesScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'color_palettes_scrren',
        style: TextStyle(
          fontSize: 42
        ),
      ),
    );
  }
}
